apply plugin: 'com.android.application'
apply plugin: 'kotlin-android'
apply plugin: 'kotlin-android-extensions'
apply plugin: 'kotlin-kapt'
apply plugin: 'com.google.gms.google-services'
apply from: '../versions.gradle'

android {
    compileSdkVersion 29
    defaultConfig {
        applicationId "com.asimqasimzade.authenteq"
        minSdkVersion 16
        targetSdkVersion 29
        versionCode 1
        versionName "1.0"
        testInstrumentationRunner "androidx.test.runner.AndroidJUnitRunner"
    }
    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'
        }
    }
    dataBinding {
        enabled = true
    }
}

dependencies {
    implementation fileTree(dir: 'libs', include: ['*.jar'])

    //Lifecycle extensions
    implementation deps.lifecycle_extensions

    //Design Support Libraries
    implementation deps.support.v4
    implementation deps.support.appcompat
    implementation deps.support.material
    implementation deps.support.cardview
    implementation deps.support.constraint_layout

    //Navigation component
    implementation deps.navigation.fragment
    implementation deps.navigation.ui_ktx

    //Firebase ML Kit, face detection libraries
    implementation deps.firebase.core
    implementation deps.firebase.vision
    implementation deps.firebase.face_model

    //Testing libraries
    testImplementation deps.test.junit
    androidTestImplementation deps.test.espresso
    androidTestImplementation deps.test.runner
    androidTestImplementation deps.test.rules

    //Camera Kit
    implementation deps.camerakit.core
    implementation deps.camerakit.jpegkit
    implementation deps.kotlin
    implementation deps.kotlin_coroutines

    //Gson
    implementation deps.gson
}

repositories {
    mavenCentral()
}
